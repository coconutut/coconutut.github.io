---
date: '2026-05-24T22:57:15+08:00'
draft: false
title: '最长公共子序列'
tags: ["多维动态规划"]
categories: ["Hot100"]
---

1. 本题为字符串相关的多维动态规划
    * 子串要连续，子序列只需顺序一致
    * 首先就是状态表示，`dp[i][j]`表示i、j长度的字符串最长的公共子序列
    * 初始化要从1开始，这样解决转移过程中`dp[i-1][j]、dp[i][j-1]`问题
```c++
class Solution {
public:
    int longestCommonSubsequence(string text1, string text2) {
        size_t size1 = text1.size();
        size_t size2 = text2.size();
        vector<vector<int>> dp(size1+1, vector<int>(size2+1, 0));
        for(int i = 1; i <= size1; i++){
            for(int j = 1; j <= size2; j++){
                //末尾相同时，是i-1、j-1时候加一
                if(text1[i-1] == text2[j-1]){
                    dp[i][j] = dp[i-1][j-1] + 1;
                }
                //末尾不相同时，去两边找最大的
                else{
                    dp[i][j] = max(dp[i-1][j], dp[i][j-1]);
                }
            }
        }
        return dp[size1][size2];
    }
};
```
